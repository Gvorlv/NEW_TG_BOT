version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"  # Замените на нужный порт
    volumes:
      - .:/app  # Для локальной разработки, уберите в продакшн версии
    environment:
      - PYTHONUNBUFFERED=1